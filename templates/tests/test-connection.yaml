apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "m3-music.fullname" . }}-test-connection"
  labels:
    {{- include "m3-music.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "m3-music.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
