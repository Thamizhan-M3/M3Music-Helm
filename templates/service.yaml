{{/* Disabled duplicate parent Service; backend/frontend services are owned by subcharts */}}
{{- /* intentionally not rendering */ -}}
{{- if false }}

apiVersion: v1
kind: Service

metadata:
  name: {{ include "m3music.fullname" . }}
  labels:
    {{- include "m3music.labels" . | nindent 4 }}

{{- /* intentionally blank */ -}}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      selector:
    {{- include "m3music.selectorLabels" . | nindent 4 }}
{{- end }}


